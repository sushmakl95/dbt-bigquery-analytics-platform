version: 2

seeds:
  - name: country_regions
    description: "ISO-2 country code → region/sub-region lookup."
    columns:
      - name: country_code
        tests: [unique, not_null]
      - name: region_name
        tests: [not_null]
